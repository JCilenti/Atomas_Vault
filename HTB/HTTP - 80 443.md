
- a folder in web app terminology is called a directory
- 404 is the HTTP response code for "Not Found"
- We will use the tool Gobuster to brute force the directories in a web app
- The `dir` switch with Gobuster allows us to search through directories, not subdomains
- 